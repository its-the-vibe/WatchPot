services:
  watchpot:
    build: .
    image: ghcr.io/its-the-vibe/watchpot:latest
    pull_policy: always
    restart: on-failure:10
    read_only: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${PORT}:8080"
    volumes:
      - ./config.yaml:/config.yaml:ro
    environment:
      - WATCHPOT_CONFIG=/config.yaml
      - REDIS_PASSWORD=${REDIS_PASSWORD}
